export const VARIANT = Object.freeze({
  LIGHT: "light",
  DARK: "dark",
  PRIMARY: "primary",
  SECONDARY: "secondary",
});

/**
 * @typedef {typeof VARIANT[keyof typeof VARIANT]} Variant
 */

export const BACKGROUND = Object.freeze({
  GLASS: "glass",
  SOLID: "solid",
  TRANSPARENT: "transparent",
});

/**
 * @typedef {typeof BACKGROUND[keyof typeof BACKGROUND]} Background
 */
