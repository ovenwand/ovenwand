/// <reference types="cypress" />

describe('@ovenwand/util.string', () => {
	describe('base64', () => {
		describe('encode', () => {
			it.skip('encodes strings to base64 format');
		});

		describe('decode', () => {
			it.skip('decodes strings from base64 format');
		});
	});
});
