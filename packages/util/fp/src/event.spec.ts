/// <reference types="cypress" />

describe('@ovenwand/util.fp', () => {
	describe('event', () => {
		describe('preventDefault', () => {
			it.skip('Prevents default behavior of cancelable event');
			it.skip('Does not prevent default behavior of non-cancelable event');
		});
	});
});
