/// <reference types="cypress" />

describe('@ovenwand/util.fp', () => {
	describe('is', () => {
		describe('isNumber', () => {
			it.skip('Returns `true` when provided value is a number');
			it.skip('Returns `false` when provided value is a number');
		});
	});
});
