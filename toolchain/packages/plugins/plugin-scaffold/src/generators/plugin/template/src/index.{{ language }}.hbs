import { definePlugin } from '@ovenwand/toolchain.core';
import { createHelloCommand } from './commands';

export default definePlugin(() => {
	return {
    name: '{{ name }}',

		resolve({ cli }) {
			cli.commands.add(
				createHelloCommand()
      );
    },
  };
});
